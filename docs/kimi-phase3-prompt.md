You are a senior Rust systems engineer running an UNATTENDED session on **Cambium** (signalbreak-labs), an order-correct YANG **library/SDK** — the goyang successor (**NOT ygot**; it does **not** send NETCONF and does **not** generate Terraform providers; those are downstream consumers — see `AGENTS.md` "Non-goals"). Work autonomously to completion; do not wait for approval between steps.

You are continuing on branch **`feat/cambium-sdk-phase1`** (current HEAD `dab9d47`). **Stay on it.** Commit each green slice separately with a Conventional-Commit subject ≤50 chars. **Do not push.** If a slice (or a sub-part of one) cannot be made confidently green, revert it with `git checkout` / `git restore`, leave the rest green, and log it under "DEFERRED" in the handoff — **never commit red, never weaken or delete a test, never guess a C struct layout or a libyang flag value.**

---

## Context — what already landed (do NOT redo)

Phases 1, 1b, and 2 are in and green. **Phase 1/1b** = `ContextBuilder` → frozen `Context`, borrowed `Module<'ctx>` / `SchemaNodeRef<'ctx>`, full `TypeInfo<'ctx>` → `ResolvedType<'ctx>` richness. **Phase 2** = the entire data-tree layer: `NodeRef`/`NodeSet`/`Value`, `get`/`try_get`/`exists`/`select`/`root_nodes`, `new_path`/`set_value`/`remove_path`/`unlink_path`/`add_defaults`, structured `ValidationErrors`/`Diagnostic` (the `thiserror` reshape with `E0006 DataPath` + `E0007 Stale`), and the read+write `UserOrderedList`/`UserOrderedLeafList` (with the post-review fix: `as_user_ordered` returns a read-only `UserOrderedView`; reordering stays on `&mut DataTree`). A follow-up adversarial review already fixed a soundness hole, an ordering bug (unfiltered child counter), a leak, and a flaky validate — do **not** revisit those.

**Phase 3 = (A) serialization completeness + (B) diff / merge / dup** — the plan/apply engine a downstream NETCONF/gNMI/Terraform consumer needs. This is the "plan/apply" win goyang/ygot never had a clean answer for. **Phase 3 is Rust-only; Go is untouched this run** (it stays cached green; you only confirm no cross-language compile drift if you reshape a public type).

**Success floor + commit order as a safety mechanism:** land and commit **Slice 1**, then **Slice 2a** (ParseMode reshape only), then **Slice 3** (`duplicate()`) — these are the floor and must be green + committed BEFORE you start Slice 4. Treat **Slice 2b** (`Format::Lyb`) and **Slices 4–5** as deferrable. Within 4–5: `diff_apply_round_trip` needs only `RawDataDiff` (no metadata read) and may land even if the `is_ordered_by_user` metadata walk is deferred — ship the apply round-trip green while `#[ignore]`-ing `diff_ordered_by_user_atomic` if the `lyd_meta` walk can't be confidently verified. Never ship red; never over-reach.

---

## READ FIRST (ground yourself in the real code before any unsafe read)

- **`spec/api.md`** §F (serialization), §G (diff/merge/dup), §I (error contract), and the "Invariant → mechanism" table (I2, I5, I6 rows) — the language-neutral contract. PRs that diverge fail review.
- **`docs/sdk-api-design.md`** §F (`Format::Lyb`, `WithDefaults` enum, the **reshaped** `SerializeFlags`, the **reshaped** `ParseMode` flags struct — lines 384–411) and §G (`DiffOpts`/`MergeOpts`/`DiffOp`, `DataDiff`/`DiffEdit`, `diff`/`diff_apply`/`merge`/`duplicate` — lines 421–456).
- **`spec/rule-codes.md`** — the append-only registry. `E0004 Serialize` and `E0006 DataPath` already exist; the merge-conflict error reuses an existing code (see LOCKED, below) — **do not renumber, do not add a new top-level code without recording it append-only.**
- **`rust/cambium-core/src/tree.rs`** — the `DataTree<'_>` you are extending. `Format` (no `Lyb` yet), `ParseMode` (the mutually-exclusive enum at lines 28–38), `SerializeFlags` (just `{siblings}` at 40–45), `DataTree::serialize` (lines 185–195, maps **only** `siblings`), and the `From<Format> for RawFormat` / `From<ParseMode> for u32` impls (lines 364–392). **Extend the existing `impl DataTree`; do not fork a parallel type.**
- **`rust/cambium-core/src/sys_consts.rs`** — the **one** module that re-exports libyang consts into the safe core. Today it has `LYD_PARSE_ONLY/OPAQ/STRICT`, `LYD_PRINT_SIBLINGS`, `LYD_VALIDATE_*`, `LYD_NEW_PATH_*`, `LYD_NEW_VAL_OUTPUT`, `LYD_IMPLICIT_*`. You add the rest here (hexagonal discipline: `cambium-core` references libyang consts **only** through `sys_consts`; never `use cambium_libyang_sys::LYD_*` directly in `tree.rs`).
- **`rust/cambium-core/src/context.rs`** — `Context::parse(format, mode, data)` (lines 139–145) is the **only** consumer of `From<ParseMode> for u32`. `parse_op` does NOT take `ParseMode`. The ctx `*const ly_ctx` is already plumbed into `RawDataTree` (Phase-2 Slice 0).
- **`rust/cambium-core/src/error.rs`** — the `thiserror` `Error` + `RuleCode` from Phase 2. Reuse `Error::ffi(RuleCode, msg)`; do not reshape it.
- **`rust/cambium-libyang-sys/src/adapter.rs`** — `RawDataTree { ptr: *mut lyd_node, ctx: *const ly_ctx }`, the **FROZEN** `serialize` / `lyd_print_mem` wrapper (≈ lines 1288–1309), `parse_data` (≈ 1001–1026), `into_raw` (nulls `self.ptr`), the `RawFormat` enum + `From<RawFormat> for LYD_FORMAT` (≈ 1756–1772), the existing `value._canonical` read with `lyd_value_get_canonical` fallback (`node_value_str_direct`, ≈ 79–85), the `lyd_first_sibling` re-anchor pattern after `new_path`/`add_defaults`/`validate` (≈ 1552/1615/1316), the `cstr_opt` helper (≈ 26), the `RawDataTree` Drop calling `lyd_free_all` (≈ 1725–1733), and the `libc::free` of `lyd_path`/serialize allocations (≈ 1305). **This is where every new raw primitive lands.**
- **`rust/cambium-libyang-sys/src/bindings.rs`** — the **authoritative struct layouts and fn signatures** (bindgen output). Confirm every field name / type / **pointer depth** AND every flag numeric value here before any unsafe read. The catalog cites line numbers — re-verify them; bindgen output can shift.
- **`rust/cambium-libyang-sys/vendor/libyang/src/{tree_data.h,printer_data.h,parser_data.h,diff.h,diff.c,plugins_exts/metadata.h}`** — the C ownership rules, the diff-metadata encoding, and the `static inline` getters you CANNOT bind.
- **`rust/conformance-runner/src/main.rs`** — parses each case with `ParseMode::DataOnly` (a P3-breaking call site, ≈ line 87) and serializes with default `SerializeFlags`. Migrate it in lockstep.
- **`rust/cambium-core/tests/data_crud.rs`** — the `cambium-data-crud-demo` module (`top { leaf enabled {boolean default "true"} leaf counter {uint64} container nested {leaf name} list item {key id; leaf value} leaf-list tags }`). **Reuse this module** for serialize/diff/merge/dup tests; the `enabled` default-true leaf is the with-defaults probe and the empty `nested` container is the keep-empty probe.
- **`conformance/fixtures/ordered-user/`** — module `ordered-user-demo` (`container config { list entry { ordered-by user; key name; leaf name; leaf value } }`), input entries in insertion order **c, a, b**. This is the I6 carrier fixture for `diff_ordered_by_user_atomic` and `merge_preserves_user_order`. **Reuse it — do not author a new ordered-by-user module.** (The design doc references a `gnmi-ordered-atomic` fixture at `sdk-api-design.md:456,637` that **does NOT exist** — the only fixtures are ietf-interfaces, keys-first, ordered-user, rpc-order, scrambled-children, system-list-canonical. Do not hunt for it or mint it; use `ordered-user`.)

---

## The goal (Phase 3 surface)

**(A) Serialization completeness** — `docs/sdk-api-design.md` §F:
- `SerializeFlags` extends `{siblings}` → `{siblings, shrink, keep_empty_containers, with_defaults: WithDefaults}`. `WithDefaults` is a **NEW enum** `{Explicit, Trim, All, AllTagged}` (not four bools).
- `ParseMode` is **RESHAPED** from the mutually-exclusive enum `{DataOnly, Strict, Opaque}` to a composable flags struct `{strict, opaque, parse_only, no_state, lyb_mod_update}`.
- `Format` gains `Lyb` (verify the vendored static build actually round-trips LYB before promising it).

**(B) Diff / merge / dup** — `docs/sdk-api-design.md` §G:
- `DataTree::diff(&self, other, DiffOpts) -> Result<DataDiff>`; `diff_apply(&mut self, &DataDiff) -> Result<()>`; `merge(&mut self, source, MergeOpts) -> Result<()>`; `duplicate(&self) -> Result<DataTree>`.
- `DataDiff` owns a yang-patch-shaped `lyd_node` tree (RAII Drop), exposes `is_empty()`, `edits() -> impl Iterator<Item = DiffEdit<'_>>` (apply-safe document order), `serialize(&self, Format) -> Result<Vec<u8>>`.
- `DiffEdit<'d>`: `op() -> DiffOp{Create|Delete|Replace|None}`, `path() -> &str`, `value() -> Option<&str>`, `is_ordered_by_user() -> bool` (the I6 carrier).

---

## LOCKED decisions (ratified — do NOT re-open; bake these in)

- **Hexagonal stays absolute.** No `lyd_node` / `ly_ctx` / `LY_ERR` / `lyd_meta` / `LYD_*` type ever crosses into `cambium-core`'s public API. FFI lives in `cambium-libyang-sys`, mapped to domain types at the adapter boundary; `cambium-core` references libyang consts **only** through `sys_consts`. **Coarse-grained:** no per-node FFI inside any loop (the diff-edit walk materializes a `Vec` of owned snapshots in ONE pass, exactly like the Phase-2 child/sibling materializer).
- **Ordering invariants are sacred.** **I2** (declaration order) and **I5** (single printer, Rust==Go bytes) are unchanged by the new print flags — `with_defaults`/`shrink`/`keep_empty_containers` are libyang print flags, **never** a re-sort. **I6**: a diff/merge edit on an `ordered-by user` list/leaf-list MUST be flagged `is_ordered_by_user()==true` and preserve libyang's `yang:insert` metadata so a consumer carries the move **atomically** (one `yang:insert`), never as N scalar updates.
- **`merge` conflict semantics are a CAMBIUM rule, not a libyang one.** libyang `lyd_merge_*` silently **overwrites** a conflicting target leaf (last-writer-wins, returns `LY_SUCCESS`). The spec rule "a conflicting leaf value is an Error (ygot semantics)" MUST be enforced in the core *before* the merge mutates `self`: pre-scan for leaves present in **both** trees with **differing** values and return `Err` without touching `self`. **`merge` reuses an existing rule code** — map the conflict to `RuleCode::Validate` (E0003) (a merge conflict is a value-consistency failure); confirm against `spec/rule-codes.md` and do not invent a new top-level code. This extends E0003's scope to the `merge` operation — make a **one-line append-only amendment** to the E0003 "Raised by" cell in `spec/rule-codes.md` adding "· merge" (an additive clarification, NOT a renumber — permitted) and record it under NEEDS HUMAN REVIEW. Implement the pre-scan by **reusing the Slice-4 diff machinery**: `diff(self, source)` then reject if any edit is a `Replace` on a path present in both trees — do NOT hand-write a fresh recursive comparator (avoids new unsafe surface).
- **`merge` preserves user-ordered order (I6).** libyang appends source siblings after target; verify with `merge_preserves_user_order`. Do not alphabetize.
- **`decimal64` stays the `{raw: i64, fraction_digits: u8}` newtype** (`Decimal64` in `tree.rs`) — unchanged.
- **`Format` and `ParseMode` are `#[non_exhaustive]`** where it matters: adding `Format::Lyb` is non-breaking for downstream matches, but every **internal** `match Format` (the two `From` impls) must add the `Lyb` arm or fail to compile.

---

## CRITICAL FFI gotchas — the Phase-3 analogue of the Phase-2 traps

These are the load-bearing traps from the verified catalog. Inline the safe pattern; do **not** rediscover them. **Verify every struct field name / type / pointer-depth AND every flag numeric value in `bindings.rs` and the vendored header BEFORE the unsafe read or the bit-OR.** The Phase-1b win came from distinguishing array-of-structs from array-of-**pointers**; the Phase-3 wins are the binary-LYB NUL truncation, the diff-tree RAII free, and reading `yang:operation`/insert metadata off the `lyd_meta` chain without leaking it.

1. **The FROZEN serialize path NUL-truncates binary LYB — `Format::Lyb` CANNOT reuse it.** `RawDataTree::serialize` (the `lyd_print_mem` wrapper) reads the printer output via `CStr::from_ptr(out).to_bytes()`, which **stops at the first 0x00 byte**. LYB is a binary format full of embedded NULs, so routing `Format::Lyb` through the frozen path silently corrupts the output. **Do NOT touch the frozen path** (that is the byte-stability gate for XML/JSON goldens). Add a **separate length-aware** primitive: `ly_out_new_memory(&mut buf, 0, &mut out)` → `lyd_print_all(out, root, LYD_LYB, options)` → read **exactly** `ly_out_printed(out)` bytes into a `Vec<u8>` (NOT `CStr`/strlen) **before freeing** → then `ly_out_free(out, …)` (verify its exact signature at `bindings.rs:7135` — `(out, destructor: Option<…>, destroy)` — pass `destructor=None` and the correct `destroy` flag for an `ly_out_new_memory` buffer rather than guessing `0`; the copy MUST precede the free, which releases the buffer). (`ly_out_new_memory` `bindings.rs:7094`, `lyd_print_all` `:7144`, `ly_out_printed` `:7129`, `ly_out_free` `:7135`.) `lyd_print_all` auto-includes siblings (no separate `siblings` flag needed for LYB); LYB ignores text print flags except SHRINK/SIBLINGS. **First write `serialize_lyb_round_trip` (parse JSON → serialize LYB → parse LYB → serialize JSON, assert equal) and watch it pass at runtime** — the build IS verified to compile/link LYB (152 lyb symbols incl `lyb_print_data`/`lyb_parse_data` in the static lib; `printer_data.c` dispatches `case LYD_LYB`), so this is a runtime-prove step, not a capability gamble. If the length-aware path cannot be made confidently green this slice, DEFER `Format::Lyb` with **that exact reason** (binary truncation / length-aware path), `#[ignore]` the test (do not delete it), and keep XML/JSON/JsonIetf shipping — do **not** ship a `CStr`-truncated LYB encoder.

2. **The byte-stability gate: default `SerializeFlags` must map to EXACTLY the current bits.** Today `DataTree::serialize` ORs **only** `LYD_PRINT_SIBLINGS` (when `flags.siblings`), and the adapter additionally force-ORs `LYD_PRINT_EMPTY_CONT` for `JsonIetf` only. The new fields must contribute **zero** bits for the default profile: `WithDefaults::Explicit → LYD_PRINT_WD_EXPLICIT == 0` (a no-op bit), `shrink:false → no SHRINK`, `keep_empty_containers:false → no EMPTY_CONT`. **Map each new flag to its bit ONLY when set**, OR them into the existing options bitmap, and leave the `JsonIetf` `EMPTY_CONT` force-OR in the adapter **untouched** (do not double-OR it). Concretely: `siblings → LYD_PRINT_SIBLINGS(1)`, `shrink → LYD_PRINT_SHRINK(2)`, `keep_empty_containers → LYD_PRINT_EMPTY_CONT(4)`, `with_defaults: Explicit→0 / Trim→LYD_PRINT_WD_TRIM(16) / All→LYD_PRINT_WD_ALL(32) / AllTagged→LYD_PRINT_WD_ALL_TAG(64)` (mutually-exclusive bits under `LYD_PRINT_WD_MASK==240`; select exactly one). **The keep-empty-container constant is named `LYD_PRINT_EMPTY_CONT` (value 4) — NOT `LYD_PRINT_KEEPEMPTYCONT`.**

3. **`SerializeFlags::default()` — you can no longer `#[derive(Default)]`.** `WithDefaults` is an enum, not a bool. **Hand-write `impl Default for SerializeFlags`.** The design-doc comment (line 400) says `siblings:true`, but the current `#[derive(Default)]` gives `siblings:FALSE` and **all 12 goldens were produced with that**. **RESOLUTION (decisive — do not waffle):** the authoritative design doc (`sdk-api-design.md:400` and `:413`, which names WITH_SIBLINGS as part of the golden profile) specifies `Default { siblings: true, shrink: false, keep_empty_containers: false, with_defaults: Explicit }`. The conformance-runner passes `SerializeFlags { siblings: true }` **explicitly** (`main.rs:96`), so it is **insensitive** to the `Default` value — which means the byte-stability gate alone CANNOT catch a wrong default. **Set `siblings: true`** to match the design doc and the bytes the goldens already encode. **CRITICAL:** write `serialize_default_bytes_unchanged` to call `tree.serialize(fmt, SerializeFlags::default())` — the `Default` impl, **NOT** an explicit `{siblings:true}` literal — so the test actually exercises the chosen `Default`; assert byte-identical to the committed golden in BOTH Xml and Json. If `siblings:true` somehow changes any golden byte, **STOP and report** — do not silently revert.

4. **`ParseMode` reshape is BREAKING — migrate every call site in lockstep, in the same commit.** The enum `{DataOnly, Strict, Opaque}` becomes `struct { strict, opaque, parse_only, no_state, lyb_mod_update }`. Rewrite `From<ParseMode> for u32` as an **OR-accumulator**: `strict?|=LYD_PARSE_STRICT(131072)`, `opaque?|=LYD_PARSE_OPAQ(262144)`, `parse_only?|=LYD_PARSE_ONLY(65536)`, `no_state?|=LYD_PARSE_NO_STATE(524288)`, `lyb_mod_update?|=LYD_PARSE_LYB_SKIP_MODULE_CHECK(1048576)`. **`Context::parse` does NOT change** (`mode.into()` still works). The breakage is the ~12 call sites constructing `ParseMode::DataOnly`. **Migration:** `ParseMode::DataOnly → ParseMode { parse_only: true, ..Default::default() }` — `LYD_PARSE_ONLY` means parse-without-validate; `Default::default()` (all-false) does NOT equal `DataOnly` (it turns validation ON), so set `parse_only: true` explicitly or fixtures that parse tolerated-invalid data start failing. Add a `ParseMode::data_only()` convenience constructor so the test diff is mechanical. Call sites to fix (grep to confirm; the catalog lists): `tests/{ordered_user.rs, keys_first.rs, user_ordered_list.rs, schema_order.rs, json_order.rs, data_read.rs, system_ordered.rs, json_ietf.rs}`, `tests/compile-fail/user_ordered_list_no_set.rs`, `cambium-codegen/tests/{codegen_v1.rs, codegen_order_demo.rs}`, `conformance-runner/src/main.rs`. **Enforce the libyang constraint:** `STRICT` and `OPAQ` MUST NOT be combined (except for LYB) — `parse_mode { strict: true, opaque: true }` is illegal; return a domain `Error` from the mapper rather than passing both bits.

5. **The `lyb_mod_update` field name does NOT match a real flag — FLAG FOR HUMAN REVIEW.** There is **no `LYD_PARSE_LYB_MOD_UPDATE`** in this pinned libyang. The closest real flag is `LYD_PARSE_LYB_SKIP_MODULE_CHECK` (value `1048576`). Map the design-doc field `lyb_mod_update` to `LYD_PARSE_LYB_SKIP_MODULE_CHECK`, document the rename in a `//` comment, and call it out under NEEDS HUMAN REVIEW. Do **not** invent a flag value.

6. **The diff out-param is an OWNED tree the caller MUST free exactly once with `lyd_free_all`.** `lyd_diff_siblings(first: *const lyd_node, second: *const lyd_node, options: u16, diff: *mut *mut lyd_node) -> LY_ERR` (`bindings.rs:5728`; also `lyd_diff_tree` `:5721`). The out `diff` is "completely independent on first and second" (`tree_data.h`). **Wrap it in a fresh `RawDataDiff { ptr, ctx }` struct with a Drop calling `lyd_free_all(ptr)` when non-null — EXACTLY mirroring `RawDataTree` Drop.** Trap: returning the raw pointer leaks it; freeing it both manually and via Drop double-frees. **`*diff == NULL` on no-difference is SUCCESS + empty edit list, NOT an error** — do not reuse the `parse_data` "null ptr → Err" pattern here. On `LY_SUCCESS` with null diff, construct an empty `DataDiff` whose `edits()` yields nothing and `is_empty()==true`. **Never `lyd_validate` the diff tree** (the header explicitly forbids it). **diff options is `u16`** (NOT `u32` like print/parse/dup): `LYD_DIFF_DEFAULTS=1`, `LYD_DIFF_META=2`. Map `DiffOpts{defaults} → LYD_DIFF_DEFAULTS`. Default diff (options=0) ignores default nodes as non-existent — that is the correct NETCONF plan/apply behavior.

7. **diff op + ordered-by-user are read off the `lyd_meta` chain — there is NO `lyd_get_meta_value` binding.** The op is `yang:operation` metadata with **string** values `"create"|"delete"|"replace"|"none"`, and it is **INHERITED from the nearest parent** (top-level nodes always carry it). The I6 signal is the presence of `yang` insert metadata named `key` (user-ordered list), `value` (user-ordered leaf-list), or `position` (positional). To read these in one coarse walk:
   - Resolve `yang_mod = ly_ctx_get_module_implemented(ctx, c"yang")` **once** per walk (cache it). Pass it (NOT NULL) to every `lyd_find_meta(node->meta, yang_mod, name)` (`bindings.rs:5800`) so a same-named non-yang annotation cannot match. `lyd_diff_find_meta` is static/internal — replicate the walk with the **public** `lyd_find_meta`.
   - Read the op via `lyd_find_meta(node.meta, yang_mod, c"operation")`; if null, walk up `(*node).parent` repeating the lookup until found (the inherited op). Map the string → `DiffOp`.
   - `lyd_get_meta_value` is `static inline` (`metadata.h:50`) — **NOT bindable, calling it fails to link.** Read the meta value the SAME way the adapter reads term values: `(*meta).value._canonical` (a `*const c_char` at offset 0 of `lyd_value`, `bindings.rs:7028`/lyd_value `:4618`); if NULL fall back to `lyd_value_get_canonical(ctx, &(*meta).value)`. Read the meta NAME via `(*meta).name`. **Copy every string out into an owned `String` inside the walk** — `DiffEdit<'d>` borrows from the owned `Vec`; the diff tree frees its `lyd_meta` on Drop, so never hold a raw `*lyd_meta` past the walk.
   - **`(*node).schema` is NULL for opaque nodes** — guard `if (*node).schema.is_null()` before any schema deref, return `is_ordered_by_user()==false`. Determine ordered-by-user via the **schema flag** `(*(*node).schema).flags & LYS_ORDBY_USER` (`LYS_ORDBY_USER == 64`; `nodetype:u16`@0 then `flags:u16`@2 on the lysc_node base, same offset across all variants) — the schema flag is canonical, because a CREATE at the end of a user-ordered list may omit insert metadata but is still ordered-by-user. Reuse the existing `SchemaNodeRef::ordered_by()` mapping at the boundary rather than re-deriving from path strings.

8. **`diff_apply` mutates in place via a double-pointer and does NOT consume the diff.** `lyd_diff_apply_all(data: *mut *mut lyd_node, diff: *const lyd_node) -> LY_ERR` (`bindings.rs:5753`). Pass `&mut self.ptr` as `data`, `diff.ptr` as `*const`. After `LY_SUCCESS`, **re-anchor** `self.ptr = lyd_first_sibling(self.ptr)` (creates/deletes move the first sibling). The diff is `const` and NOT freed — the `DataDiff` still owns it; do NOT double-free, leave it usable so `diff_apply_round_trip` can re-inspect it. Caution: a diff built WITHOUT `LYD_DIFF_DEFAULTS` may create duplicate values on apply; do not chain further applies without a `validate`.

9. **`merge` double-pointer re-anchor + `LYD_MERGE_DESTRUCT` double-free.** `lyd_merge_siblings(target: *mut *mut lyd_node, source: *const lyd_node, options: u16) -> LY_ERR` (`bindings.rs:5697`; also `lyd_merge_tree` `:5690`). **options is `u16`** (cast the `u32` consts): `LYD_MERGE_DESTRUCT=1`, `LYD_MERGE_DEFAULTS=2`, `LYD_MERGE_WITH_FLAGS=4`. Pass `&mut self.ptr` as `target`; after `LY_SUCCESS` re-anchor `self.ptr = lyd_first_sibling(self.ptr)`. **`LYD_MERGE_DESTRUCT` "spends" (frees) the source** — if you pass it, the Rust `DataTree` wrapping source must NOT also Drop it. The design signature is `merge(&mut self, source: &DataTree, ...)` (source is BORROWED) — you **cannot** null a `&DataTree`'s ptr. **Safe default for `MergeOpts{destruct}`: dup the source with `lyd_dup_siblings` then merge-DESTRUCT the COPY**, so the caller's borrowed source is never mutated/freed regardless of the `destruct` flag. (If you ever expose a by-value destruct path, call `source.into_raw()` to null the consumed ptr exactly like the existing insert pattern.) **Remember the conflict pre-scan (LOCKED) runs BEFORE any mutation** — `lyd_merge_*` itself will not surface a value conflict; prove this with `merge_conflict_errors` driven through real in-memory trees, not by assuming libyang returns `LY_EVALID`.

10. **`duplicate()` MUST use `lyd_dup_siblings` + `LYD_DUP_RECURSIVE`, or it is silently shallow / drops roots.** `lyd_dup_single` copies only ONE root (drops every other top-level sibling of a multi-container/multi-module tree). Without `LYD_DUP_RECURSIVE=1` you get a one-node shallow copy. Safe pattern: `first = lyd_first_sibling(self.ptr)`; `lyd_dup_siblings(first, NULL /*standalone, no WITH_PARENTS*/, LYD_DUP_RECURSIVE, &mut out)` (`bindings.rs:5666`); wrap `out` in a fresh `RawDataTree { ptr: out, ctx: self.ctx }` with its own Drop. **Keep metadata** — do NOT set `LYD_DUP_NO_META` (user-ordered/default metadata must survive for I6 + add_defaults). Consider `LYD_DUP_WITH_FLAGS=8` to preserve validated/default node state. dup options is **`u32`**. The dup shares the same `ly_ctx` (schema refs identical) — keep the `Context` alive for the dup's lifetime (the same-context invariant; the `'ctx` lifetime already enforces this).

11. **Cross-context diff/merge is UB — assert pointer-equal ctx at the core boundary.** `lyd_diff_*`/`lyd_merge_*` compare schema pointers within one `ly_ctx`. Before any diff/merge FFI call, assert `self` and `other`/`source` share the same `Context` (the `'ctx` lifetime should already guarantee this since both borrow the same `&Context`; if the raw ctx pointers are reachable, assert pointer-equal and return `Error::ffi(RuleCode::DataPath, "cross-context …")` otherwise). Do not write a test mixing contexts.

12. **`WithDefaults::All` requires materialized defaults.** `LYD_PRINT_WD_ALL=32` prints all default nodes, but there is nothing to print unless `add_defaults` materialized them first. A `serialize_with_defaults_all` test that never calls `add_defaults` silently passes a no-op. The WD::All test MUST `add_defaults(ImplicitOpts::default())` first, then assert the default leaf (`enabled=true`) appears **in declaration order**. Build the tree WITHOUT explicitly setting `enabled` so it is genuinely a default (otherwise Trim/All are indistinguishable from Explicit). `keep_empty_containers` must likewise be tested on `Format::Xml`/`Json` (where empty non-presence containers are pruned) — NOT on `JsonIetf`, which already force-keeps them and would pass vacuously.

13. **Leak / RAII discipline (carried forward).** Every `lyd_path()` result → `libc::free`. Every owned tree (diff out, dup out, merge-dup) → exactly one `RawDataTree`/`RawDataDiff` Drop calling `lyd_free_all`. The diff tree and the dup tree are independently owned (separate frees). If a find helper allocates an `ly_set`, free it with a **NULL destructor** (`ly_set_free(set, None)`) so the set struct frees but not the borrowed nodes — but diff/merge/dup do not return `ly_set`. Flag every new `unsafe` block for human review.

---

## NON-NEGOTIABLE gates (a slice that can't meet these is reverted and logged DEFERRED)

1. **TDD red-first.** Add the failing test FIRST, watch it fail, **then** implement. New integration tests live in `rust/cambium-core/tests/*.rs` (one file per area is fine: `serialize_flags.rs`, `parse_mode.rs`, `diff.rs`, `merge.rs`, `duplicate.rs`). Reuse the existing harness boilerplate verbatim (`ContextBuilder` → `search_path` → `load_module` → `parse(...)`; see `data_crud.rs`) and **inline** every module string — never depend on a `target/` artifact, never commit a fixture under `target/`. Reuse `cambium-data-crud-demo` and the `ordered-user` fixture rather than minting new modules.
2. **THE PHASE-3 BYTE-STABILITY GATE (load-bearing).** Default `SerializeFlags` must leave **`/conformance/golden` byte-identical** — all 12 golden files (6 cases × {xml, json}) unchanged — and the conformance-runner must still print **all 6 cases PASS**. **Only NEW flag combinations may change bytes.** Do **NOT** modify the FROZEN `RawDataTree::serialize` / `lyd_print_mem` wrapper for the XML/JSON/JsonIetf path; the LYB primitive is a SEPARATE function (gotcha #1). Write `serialize_default_bytes_unchanged` (asserts `SerializeFlags::default()` over a representative built tree is byte-identical to the committed golden in BOTH Xml and Json) as the freeze anchor, and re-run `cargo run -p conformance-runner` after every serialize/parse change. **Never edit or regenerate any `/conformance/golden/` file.**
3. **Rust-bytes == Go-bytes determinism gate still holds.** Go is untouched in P3, but do not regress the shared invariant: JSON object member order is the single printer's order. If you reshape a public type (`ParseMode`/`SerializeFlags`/`Format`), run `(cd go && CGO_ENABLED=1 go build ./... && go test ./...)` once to confirm no cross-language compile drift — Go should be cached green. Note in the handoff that the cross-language "composable flag struct" contract (`api.md` §F) now drifts (Go `ParseMode` is still the old enum) — **flag it for the Go-parity phase**, do not change Go.
4. **Trust the compiled runtime, not header comments.** Confirm what libyang actually returns via a test before encoding an assumption (the diff null-on-no-diff, the LYB round-trip, the merge silent-overwrite, the `lyd_change_term`-style triads). The merge-conflict-is-not-a-libyang-error fact in particular MUST be proven by a real test, not assumed.
5. **Green before every commit:** `cargo build --workspace` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo test --workspace` · `cargo run -p conformance-runner`.
6. **Hexagonal + house style.** Raw `lyd_*`/`lyd_meta`/`LY_ERR` extraction stays in `cambium-libyang-sys`, mapped to domain types at the boundary; none appear in `cambium-core`'s public API. No `unwrap`/`expect` outside tests; propagate with `?`; `thiserror` for the error enum; enums over `bool`+`Option`; `#![deny(missing_docs)]` on public API; `cargo fmt --all` before the final commit. `cambium-core` references libyang consts **only** through `sys_consts` — add `LYD_PRINT_SHRINK`, `LYD_PRINT_EMPTY_CONT`, `LYD_PRINT_WD_TRIM/ALL/ALL_TAG`, `LYD_PARSE_NO_STATE`, `LYD_PARSE_LYB_SKIP_MODULE_CHECK` there. The diff/merge/dup **option** consts (`LYD_DIFF_*`, `LYD_MERGE_*`, `LYD_DUP_*`) are consumed **adapter-side** (B is implemented in the adapter) and need NOT be re-exported through `sys_consts`.
7. **Keep the existing public API working.** Every Phase-1/2 surface keeps compiling: schema handles, `NodeRef`/`NodeSet`/`Value`, the CRUD mutators, `validate`/`serialize`/`user_ordered_list_at`, the `UserOrderedView` read side. **Extend `impl DataTree`; do not fork.** The only intentional break is `ParseMode` (reshape) — migrated in lockstep within its slice.
8. **Flag every new FFI/`unsafe` diff** in the handoff under "NEEDS HUMAN REVIEW" — especially: the LYB length-aware print primitive (`ly_out_new_memory`/`lyd_print_all`/`ly_out_printed`, NOT `CStr`); the `RawDataDiff` RAII Drop + the null-diff-is-empty branch; the `yang:operation` inherited-from-parent walk + the `(*meta).value._canonical` read + the one-coarse-walk attestation; the ordered-by-user schema-flag detection (`LYS_ORDBY_USER`); the merge dup-then-DESTRUCT ownership choice + the conflict pre-scan; the `lyd_dup_siblings`+`RECURSIVE` choice; the diff/merge `u16` options cast; the `lyb_mod_update → LYD_PARSE_LYB_SKIP_MODULE_CHECK` rename; and every new `sys_consts` constant. **Because tests cannot observe FFI call-count**, paste the diff-edit materialization loop into the handoff and explicitly attest there is **no `lyd_*`/FFI call inside the loop body** (the single coarse walk takes the root once and returns a `Vec` of owned `RawDiffEdit` snapshots).
9. **Existing Rust tests stay green — including assertions OUTSIDE `/conformance`.** The `ParseMode` reshape touches test files that assert ordered child vectors and golden bytes — migrate them mechanically (`ParseMode::DataOnly → ParseMode::data_only()`) in the same commit; do not weaken any assertion.

---

## Ordered slices (easiest / most self-contained FIRST — each independently green & committed)

### Slice 1 — `SerializeFlags` completeness + the byte-stability gate  ← THE FREEZE ANCHOR
The most self-contained: pure additive flag mapping, no new ownership.
- **`sys_consts.rs`:** add `pub use` of `LYD_PRINT_SHRINK`, `LYD_PRINT_EMPTY_CONT`, `LYD_PRINT_WD_TRIM`, `LYD_PRINT_WD_ALL`, `LYD_PRINT_WD_ALL_TAG`.
- **`tree.rs`:** add the `WithDefaults { Explicit, Trim, All, AllTagged }` enum (`#[non_exhaustive]`, with a `#[derive(Default)] → Explicit` or a hand `Default`). Extend `SerializeFlags` to `{ siblings, shrink, keep_empty_containers, with_defaults }`; **hand-write `impl Default`** (gotcha #3). Rewrite `DataTree::serialize`'s options builder to OR in each new bit ONLY when set (gotcha #2); leave the adapter's `JsonIetf` `EMPTY_CONT` force-OR untouched.
- **Red tests (write first):** `serialize_default_bytes_unchanged` (the freeze anchor — byte-identical to the committed golden, Xml + Json), `serialize_with_defaults_explicit_equals_today`, `serialize_with_defaults_all_shows_default_leaves` (after `add_defaults`, declaration order), `serialize_with_defaults_trim_hides_default_leaves`, `serialize_keep_empty_container` (Xml/Json, empty `top/nested`; pruned when false), `serialize_shrink_removes_insignificant_whitespace`.
- **Gate:** re-run `conformance-runner` (6/6) and confirm goldens byte-unchanged before committing.

### Slice 2a — `ParseMode` → composable flags struct (BREAKING — MUST LAND; its own commit)
- **`ParseMode` reshape (gotcha #4, #5):** change the enum to the flags struct `{ strict, opaque, parse_only, no_state, lyb_mod_update }`; rewrite `From<ParseMode> for u32` as an OR-accumulator with the STRICT+OPAQ mutual-exclusion guard returning `Err`; add `ParseMode::data_only()`; add `LYD_PARSE_NO_STATE` + `LYD_PARSE_LYB_SKIP_MODULE_CHECK` to `sys_consts`. **Migrate every `ParseMode::DataOnly` call site + the conformance-runner in the SAME commit** (a partial migration is a red workspace that masks the real P3 work).
- **Red tests:** `parse_mode_compose_strict_plus_no_state` (the composability the old enum could not express; STRICT rejects an unknown node, NO_STATE honored), `parse_mode_strict_opaque_rejected` (the mutual-exclusion guard returns `Err`).

### Slice 2b — `Format::Lyb` (DEFERRABLE; separate commit)
- **`Format::Lyb` (gotcha #1):** add `Format::Lyb` + `RawFormat::Lyb → LYD_LYB(3)` + the `Lyb` arm in both `From` impls; add the SEPARATE length-aware LYB serialize primitive (`ly_out_new_memory`/`lyd_print_all`/`ly_out_printed`/`ly_out_free`). Route `Format::Lyb` through the new primitive, NEVER the frozen `CStr` path.
- **Red test:** `serialize_lyb_round_trip` (parse JSON → serialize LYB → parse LYB → serialize JSON, equal — prove at runtime).
- **ALL-OR-NOTHING for the variant:** a `Format::Lyb` variant with a missing `From`-impl match arm is a workspace-blocking compile error. If you DEFER this slice, **EITHER** omit the `Format::Lyb` variant entirely **OR** add the `Lyb` arm to **both** `From` impls returning `Err("LYB deferred")` — never a half-added variant. Defer with the binary-truncation reason and `#[ignore]` the round-trip test (do not delete it); Slice 2a still ships.

### Slice 3 — `duplicate()` (self-contained, no metadata)
The cleanest of the (B) operations — one FFI call, one owned tree, no meta reading.
- **Adapter:** `RawDataTree::duplicate` via `lyd_dup_siblings(lyd_first_sibling(ptr), NULL, LYD_DUP_RECURSIVE [| LYD_DUP_WITH_FLAGS], &mut out)`; wrap `out` in a fresh `RawDataTree`. (gotcha #10)
- **Core:** `DataTree::duplicate(&self) -> Result<DataTree>`.
- **Red tests:** `duplicate_deep_copy_independent` (mutate the copy via `set_value`/`new_path`/`remove_path` → original unchanged, and vice versa; recursive over nested container + list), `duplicate_preserves_user_order` (the `ordered-user` fixture's c/a/b survives), `datadiff_freed_once`-style RAII check for the dup (create/drop in a loop, no leak/double-free).

### Slice 4 — `diff` + `DataDiff`/`DiffEdit` + the metadata read (THE GNARLIEST — I6 carrier)
- **Adapter:** `RawDataDiff { ptr, ctx }` with a Drop calling `lyd_free_all` (gotcha #6). `RawDataTree::diff(other, options: u16) -> Result<Option<RawDataDiff>>` (null diff → `None`/empty). A **single coarse pre-order walk** of the diff tree materializing `Vec<RawDiffEdit { op: RawDiffOp, path: String, value: Option<String>, is_user_ordered: bool, insert_key: Option<String>, … }>`: per node resolve the inherited `yang:operation` (walk up parent), read `(*meta).value._canonical`, probe `yang` `key`/`value`/`position` insert meta, read the `LYS_ORDBY_USER` schema flag (guard null schema), build the path via `lyd_path(node, LYD_PATH_STD, NULL, 0)` then `libc::free` it. **No FFI inside the loop body beyond the per-node reads of THIS walk — attest it in the handoff.** (gotcha #7)
- **Core:** `DiffOpts { defaults }`, `DiffOp { Create, Delete, Replace, None }` (`#[non_exhaustive]`), `DataDiff` (owns the `Vec<RawDiffEdit>` + the `RawDataDiff` for `serialize`), `DiffEdit<'d>` borrowing the owned `Vec` (`op`/`path`/`value`/`is_ordered_by_user`), `DataTree::diff`, `DataDiff::is_empty`/`edits`/`serialize`. Re-export the new types through all **three** re-export sites (see Facade / prelude wiring) in this same commit.
- **Red tests:** `diff_empty_when_equal` (idempotency anchor), `diff_create_delete_replace` (Create/Delete/Replace edits with correct `path()`/`value()`; None excluded from `edits()`), `diff_ordered_by_user_atomic` — **non-tautological**: build the `after` tree via `duplicate()` then `move_before`/`move_after` on the user-ordered list (`user_ordered_list_at` on the `&mut` copy) so the ONLY delta is positional (change NO leaf value); then assert ALL of `diff.edits().count() == 1`, the single edit `is_ordered_by_user() == true`, AND **no** edit has `op() == DiffOp::Replace` on a child leaf (the count + negative assertions prove the I6 atomic-carry — without them the test passes even if libyang emits N scalar updates), `datadiff_freed_once` (RAII).

### Slice 5 — `diff_apply` + `merge` (built on Slice 4)
- **Adapter:** `RawDataTree::diff_apply(&mut self, diff: &RawDataDiff)` (`lyd_diff_apply_all`, double-pointer re-anchor, diff NOT freed — gotcha #8); `RawDataTree::merge(&mut self, source, options: u16)` (dup-then-DESTRUCT-the-dup ownership, double-pointer re-anchor — gotcha #9).
- **Core:** `MergeOpts { destruct }`; `DataTree::diff_apply(&mut self, &DataDiff)`; `DataTree::merge(&mut self, source: &DataTree, MergeOpts)` with the **conflict pre-scan** running BEFORE any mutation (LOCKED; map to `RuleCode::Validate`). Assert same-context (gotcha #11).
- **Red tests:** `diff_apply_round_trip` (`a.diff(b)` then `a.diff_apply(&diff)` → `a` serializes byte-identical to `b`, default flags, Json+Xml; covers create+delete+replace in one tree), `merge_preserves_user_order` (insertion order c,a,b survives; new user-ordered entries appended positionally, not alphabetized), `merge_conflict_errors` — assert THREE things: (1) `merge()` returns `Err` with `rule_code() == RuleCode::Validate`, (2) the conflict is real (`top/counter = 1` in target vs `2` in source), and (3) AFTER the `Err`, `target.get(".../top/counter")` STILL reads `1` (the pre-scan ran BEFORE any mutation; `self` untouched). Do NOT assume libyang surfaces the conflict — it silently overwrites (`LY_SUCCESS`); the `Err` is entirely from the cambium pre-scan.

### Facade / prelude wiring (fold into the slice that adds each type — NOT a separate milestone)
As each slice adds public types (`WithDefaults`, `DiffOpts`, `MergeOpts`, `DiffOp`, `DataDiff`, `DiffEdit`, the reshaped `ParseMode`/`SerializeFlags`, `Format::Lyb`), thread every one through **ALL THREE** re-export sites in that **same** commit — missing any one silently drops the type from the facade or fails to compile:
1. `rust/cambium-core/src/lib.rs` — the `pub use crate::tree::{…}` / `pub use list::{…}` blocks.
2. `rust/cambium/src/lib.rs` — the top-level `pub use cambium_core::{…}` block.
3. `rust/cambium/src/lib.rs` — the `pub mod prelude { pub use cambium_core::{…} }` module.
`grep "pub use" rust/cambium-core/src/lib.rs rust/cambium/src/lib.rs` and confirm each new type appears in all three. Keep the legacy surfaces present.

---

## Consciously EXCLUDED from this run
- **Go parity.** Go's `ParseMode`/`SerializeFlags`/`Format` enums are untouched; the cross-language "composable flag struct" contract drifts after the Rust reshape — flag it for the Go-parity phase, do not change Go.
- **Typed-struct codegen (§H) diff/marshal routing** and **ygnmi-style path builders** — out of scope for P3 (A)+(B).
- **Per-variant `lyd_value` union reads** for diff/meta values — use the canonical-string (`_canonical` / `lyd_value_get_canonical`) path, exactly as Phase 2 does; defer behind it and flag if a test ever demands per-variant.
- **`LYD_DIFF_META` rich-metadata diffs and `diff_reverse`** — only `LYD_DIFF_DEFAULTS` is wired via `DiffOpts{defaults}`; reverse-diff is not in the §G surface.

---

## Deliverable
Run `cargo fmt --all`, make a final commit (do **not** push), and create `docs/handoff-overnight-phase3.md` (cross-linked from `docs/handoff-overnight-phase2.md`) recording, per slice: what landed (with green-bar command output for `build`/`clippy`/`test`/`conformance-runner`), what is **DEFERRED** and exactly why (especially `Format::Lyb` if the length-aware path didn't land, and any diff-metadata read that couldn't be verified), the **NEEDS HUMAN REVIEW** list of every new FFI/`unsafe` diff (call out: the LYB length-aware primitive vs the frozen `CStr` path; the `RawDataDiff` Drop + null-diff-is-empty; the `yang:operation` inherited walk + `(*meta).value._canonical` read + the one-coarse-walk attestation paste; the `LYS_ORDBY_USER` detection; the merge dup-then-DESTRUCT + conflict pre-scan; the `lyd_dup_siblings`+`RECURSIVE` choice; the diff/merge `u16` cast; the `lyb_mod_update → LYD_PARSE_LYB_SKIP_MODULE_CHECK` rename; every new `sys_consts` const), explicit confirmation that **`/conformance` goldens are byte-unchanged AND the runner prints all 6 PASS**, the cross-language drift note for the Go-parity phase, and the **exact next red test**. **Do not push.**